apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: n8n-prod

resources:
  - ../../base
  - ingress.yaml
  - n8n-postgres-sealed-secret.yaml

patchesStrategicMerge:
  - deployment-patch.yaml